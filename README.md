# Prozorro Tracker: Логіка відстеження та архітектура

Цей додаток призначений для глибокого моніторингу закупівель конкретних замовників (Військових частин) без використання проксі-серверів та з обходом обмежень CORS.

## 🛠 Технічна логіка (Data Mirror)

Оскільки API Prozorro блокує запити безпосередньо з веб-браузерів, додаток використовує архітектуру **Data Mirror** на базі GitHub Actions.

### 1. Серверний збір та архівування (GitHub Actions)
Кожні 30 хвилин (або при ручному запуску) активується воркфлоу `.github/workflows/update_tenders.yml`:
- **Накопичувальна логіка**: Скрипт спочатку завантажує існуючий файл `data/tenders.json`, а потім додає до нього нові знайдені тендери.
- **Архівування**: Це дозволяє зберігати історію закупівель. Навіть якщо тендер зник з першої сторінки Prozorro, він залишиться у вашому персональному архіві у файлі `tenders.json`.
- **Дедуплікація та ліміти**: Додаток автоматично видаляє повтори та зберігає до 500 найновіших записів для цільових організацій.
- **Глибоке сканування**: Скрипт сканує останні події на Prozorro, щоб гарантовано зафіксувати активність В/Ч.

### 2. Відображення в PWA (Клієнт)
Коли ви відкриваєте додаток на iPhone або в браузері:
- **Локальний запит**: Додаток завантажує `data/tenders.json` зі свого ж домену. Це забезпечує 100% сумісність та відсутність CORS-помилок.
- **Миттєвий доступ**: Всі тендери вже знаходяться у цьому файлі. Пошук та фільтрація відбуваються миттєво без звернення до мережі.
- **Офлайн-режим**: Service Worker (`sw.js`) використовує стратегію **Network-First** для файлу даних. Якщо у вас є інтернет — ви бачите найсвіжіші дані, якщо немає — миттєво показується остання завантажена копія.

## 📋 Функції відстеження

### Моніторинг замовників
- Додаток автоматично розпізнає цільові ЕГРПОУ.
- Всі тендери від В/Ч А4533 та А1124 відображаються у загальній стрічці та підсвічуються.
- Ви можете додати будь-якого іншого замовника через розділ "Стеження", і додаток буде шукати його закупівлі серед завантаженого масиву даних.

### Деталізація тендера
Кожна картка тендера містить:
- **Код ДК 021:2015**: Класифікація предмета закупівлі.
- **Тип процедури**: Вид торгів (Відкриті торги, Спрощена, Звіт про договір).
- **Бюджет**: Чітке відображення суми.
- **Статус**: Кольоровий індикатор (Активний, Завершений, Відмінений).

## 🚀 Як оновити дані вручну
Якщо ви не хочете чекати 30 хвилин:
1. Зайдіть у свій репозиторій на GitHub.
2. Перейдіть у вкладку **Actions**.
3. Виберіть **"Update Prozorro Tenders Data"**.
4. Натисніть **Run workflow** -> **Run workflow**.

Через 1-2 хвилини дані в додатку оновляться автоматично.
